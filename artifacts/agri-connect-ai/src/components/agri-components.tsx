import { useMemo, useState } from 'react';
import { Link, useLocation } from 'wouter';
import { AreaChart, Area, CartesianGrid, XAxis, YAxis, Tooltip, ResponsiveContainer, BarChart, Bar } from 'recharts';
import { Bell, Bot, ChevronDown, CircleHelp, CircleUserRound, Menu, Mic, Search, Send, Settings, ShoppingCart, Sparkles, X, type LucideIcon, LayoutDashboard, Wheat, CircleDollarSign, TrendingUp, ChartNoAxesCombined, ClipboardList, WalletCards, Truck, BadgeCheck, Star, Medal, LifeBuoy, ShoppingBasket, History, PiggyBank, UsersRound, FileText, Navigation, HandHelping, MapPin, Clock3, ArrowUpRight, Check, Phone, MessageCircle, CreditCard, Banknote, Smartphone, ShieldCheck, Plus, Minus, PackageCheck } from 'lucide-react';
import { crops, demandByLocation, navByRole, orders, priceHistory, products, roleInfo, type Role } from '@/data/mockData';
import { VoiceAssistant } from '@/components/VoiceAssistant';

const iconMap: Record<string, LucideIcon> = { LayoutDashboard, Wheat, CircleDollarSign, TrendingUp, ChartNoAxesCombined, ClipboardList, WalletCards, Truck, BadgeCheck, Star, Medal, LifeBuoy, Bot, ShoppingBasket, History, PiggyBank, UsersRound, FileText, Navigation, HandHelping };
const money = (value: number) => `₹${value.toLocaleString('en-IN')}`;

export function Brand({ compact = false }: { compact?: boolean }) {
  return <Link href="/" className="flex items-center gap-2.5" data-testid="link-brand"><span className="grid h-9 w-9 place-items-center rounded-xl bg-[hsl(var(--accent))] text-[hsl(var(--primary))] font-display text-lg shadow-sm">A</span><span className={compact ? 'hidden sm:block' : ''}><span className="block font-display text-[17px] leading-none">AgriConnect</span><span className="mt-0.5 block font-mono text-[9px] uppercase tracking-[.22em] text-[hsl(var(--muted-foreground))]">AI / market bridge</span></span></Link>;
}

export function AgriShell({ role, children }: { role: Role; children: React.ReactNode }) {
  const [location, setLocation] = useLocation();
  const [open, setOpen] = useState(false);
  const [chat, setChat] = useState(false);
  const [toast, setToast] = useState('');
  const [query, setQuery] = useState('');
  const info = roleInfo[role];
  const nav = navByRole[role];
  const showToast = (message: string) => { setToast(message); window.setTimeout(() => setToast(''), 2400); };
  return <div className="agri-noise min-h-[100dvh] bg-[hsl(var(--background))] text-[hsl(var(--foreground))]">
    <aside className={`fixed inset-y-0 left-0 z-40 w-[254px] -translate-x-full bg-[hsl(var(--sidebar))] px-4 py-5 text-[hsl(var(--sidebar-foreground))] transition-transform lg:translate-x-0 ${open ? 'translate-x-0' : ''}`}>
      <div className="flex items-center justify-between px-2"><Brand /><button onClick={() => setOpen(false)} className="rounded-lg p-2 lg:hidden" data-testid="button-close-menu"><X size={18} /></button></div>
      <div className="mt-7 rounded-2xl border border-[hsl(var(--sidebar-border))] bg-[hsl(var(--sidebar-accent))] p-3"><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-full bg-[hsl(var(--sidebar-primary))] font-display text-sm text-[hsl(var(--sidebar-primary-foreground))]">{info.initials}</div><div className="min-w-0"><p className="truncate text-sm font-bold">{info.name}</p><p className="truncate text-[11px] text-[hsl(var(--sidebar-foreground)/.65)]">{info.subtitle}</p></div></div></div>
      <p className="mb-2 mt-7 px-3 font-mono text-[10px] uppercase tracking-[.16em] text-[hsl(var(--sidebar-foreground)/.5)]">Workspace</p>
      <nav className="space-y-1">{nav.map(item => { const Icon = iconMap[item.icon]; const active = location === item.path; return <Link key={item.path} href={item.path} onClick={() => setOpen(false)} className={`flex items-center gap-3 rounded-xl px-3 py-2.5 text-[13px] font-medium ${active ? 'bg-[hsl(var(--sidebar-primary))] font-bold text-[hsl(var(--sidebar-primary-foreground))]' : 'text-[hsl(var(--sidebar-foreground)/.72)] hover:bg-[hsl(var(--sidebar-accent))] hover:text-[hsl(var(--sidebar-foreground))]'}`} data-testid={`link-nav-${item.label.toLowerCase().replaceAll(' ', '-')}`}><Icon size={17} strokeWidth={active ? 2.5 : 1.8} /><span>{item.label}</span></Link>; })}</nav>
      <div className="absolute bottom-5 left-4 right-4 rounded-2xl bg-[hsl(var(--sidebar-accent))] p-3"><div className="flex items-start gap-2"><Sparkles size={16} className="mt-0.5 text-[hsl(var(--sidebar-primary))]" /><div><p className="text-xs font-semibold">AI insights are ready</p><p className="mt-1 text-[11px] leading-4 text-[hsl(var(--sidebar-foreground)/.6)]">Predictions are marked as demo data.</p></div></div></div>
    </aside>
    <div className="lg:pl-[254px]"><header className="sticky top-0 z-30 flex h-[72px] items-center justify-between border-b border-[hsl(var(--border))] bg-[hsl(var(--background)/.92)] px-4 backdrop-blur-xl sm:px-7"><div className="flex items-center gap-3"><button onClick={() => setOpen(true)} className="rounded-xl p-2 hover:bg-[hsl(var(--muted))] lg:hidden" data-testid="button-open-menu"><Menu size={20} /></button><div className="relative hidden w-64 sm:block"><Search className="absolute left-3 top-1/2 -translate-y-1/2 text-[hsl(var(--muted-foreground))]" size={16} /><input value={query} onChange={e => setQuery(e.target.value)} placeholder="Search your workspace" className="h-10 w-full rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] pl-9 pr-3 text-sm outline-none focus:border-[hsl(var(--primary))]" data-testid="input-global-search" /></div><span className="hidden rounded-full bg-[hsl(var(--accent)/.28)] px-3 py-1 font-mono text-[10px] uppercase tracking-wider text-[hsl(var(--primary))] md:inline">Demo workspace</span></div><div className="flex items-center gap-1.5"><button onClick={() => showToast('You are all caught up')} className="relative rounded-xl p-2.5 hover:bg-[hsl(var(--muted))]" data-testid="button-notifications"><Bell size={19} /><span className="absolute right-2 top-2 h-1.5 w-1.5 rounded-full bg-[hsl(var(--accent))]" /></button><button onClick={() => showToast('Settings panel is ready for this demo')} className="rounded-xl p-2.5 hover:bg-[hsl(var(--muted))]" data-testid="button-settings"><Settings size={19} /></button><div className="ml-1 hidden h-8 w-px bg-[hsl(var(--border))] sm:block" /><button onClick={() => showToast(`${info.name}'s profile selected`)} className="flex items-center gap-2 rounded-xl px-2 py-1.5 hover:bg-[hsl(var(--muted))]" data-testid="button-profile"><div className="grid h-8 w-8 place-items-center rounded-full bg-[hsl(var(--secondary))] font-mono text-[10px] font-bold text-[hsl(var(--primary))]">{info.initials}</div><ChevronDown size={15} className="hidden sm:block" /></button></div></header><main className="mx-auto max-w-[1480px] p-4 pb-28 sm:p-7 lg:p-9">{role === 'farmer' && location === '/farmer/dashboard' && <VoiceAssistant onNavigate={setLocation} onOpenChat={() => setChat(true)} />}{children}</main></div>
    <button onClick={() => setChat(true)} className="fixed bottom-5 right-5 z-30 flex items-center gap-2 rounded-full bg-[hsl(var(--primary))] px-4 py-3 text-sm font-bold text-[hsl(var(--primary-foreground))] shadow-lg hover:-translate-y-0.5" data-testid="button-open-agribot"><Bot size={18} /> <span className="hidden sm:inline">Ask AgriBot</span></button>
     {chat && <Chat role={role} onClose={() => setChat(false)} onToast={showToast} />}
    <Toast message={toast} />
  </div>;
}

function getChatResponse(text: string, role: Role) {
  const query = text.toLowerCase();
  const isPrice = query.includes('price') || query.includes('cheap');
  const isOrder = query.includes('order');
  const isDelivery = query.includes('track') || query.includes('delivery') || query.includes('route');
  const isPayment = query.includes('payment') || query.includes('pay');
  const isHelp = query.includes('help') || query.includes("don't know") || query.includes('cannot') || query.includes("can't");
  const isVolunteer = query.includes('volunteer') || query.includes('assistance');
  const isBuyer = query.includes('buyer') || query.includes('farmer');

  if (role === 'farmer') {
    if (query.includes('predict')) return 'Based on the demo price trend, tomato is currently ₹30/kg and the predicted next-month price is ₹34/kg. Prediction confidence is 82%. This is a simulated AI prediction.';
    if (isPrice) return 'Based on the current demo market data, tomato is ₹30/kg in your selected locality. The recent trend is increasing. This is demo data, not live market information.';
    if (query.includes('demand')) return 'In your selected locality, tomato, chilli, and rice currently show high demo demand. Tomato has an estimated demand level of 87%.';
    if (isOrder) return 'You currently have 8 active orders and 42 delivered orders. Your next active order is #AG1024 for 800 kg of tomatoes. The order is currently in transit.';
    if (isDelivery) return 'Order #AG1024 is currently in transit. The demo tracking route is Farm → Collection Center → Transport → Consumer. Estimated delivery is 8 Sept.';
    if (isPayment) return 'Your demo payment summary shows ₹1,84,500 total earnings and ₹32,000 pending payments.';
    if (isVolunteer || isHelp) return 'No problem. You can request volunteer assistance. A volunteer can help you list crops, check prices, place orders, use payments, or track deliveries.';
    if (isBuyer) return 'I found simulated buyer interest for tomato, chilli, and rice near Guntur. Open Find Buyers to compare quantity, target price, and delivery requirements.';
  }

  if (role === 'consumer') {
    if (query.includes('save')) return 'Your current demo savings are ₹3,240. You also have approximately ₹450 in upcoming savings.';
    if (query.includes('cheap') || query.includes('tomato')) return 'I found demo tomato offers starting at ₹29/kg. The example retail comparison is ₹35/kg, giving an estimated saving of ₹6/kg.';
    if (isPayment) return 'For Individual Consumers, the total order amount must be paid at checkout. Available demo payment methods are UPI, Card, and Digital Wallet.';
    if (isDelivery) return 'Your tomato order is currently out for delivery. The estimated delivery status will update as the delivery person progresses through the route.';
    if (isOrder) return 'You have 2 active orders and 12 delivered orders. Your next delivery is currently out for delivery.';
    if (isPrice) return 'The current demo tomato price is ₹30/kg, with direct-from-farm savings shown against the example retail price.';
  }

  if (role === 'bulk') {
    if (query.includes('advance')) return 'For a bulk order, you can choose either 40% advance or full payment. For a ₹1,20,000 order, the 40% advance is ₹48,000 and the remaining balance is ₹72,000.';
    if (query.includes('pending') && isPayment) return 'You currently have bulk payment records with advance and balance amounts. Open Payments to view the complete order-wise breakdown.';
    if (isPayment) return 'Bulk buyers can choose 40% advance or full payment through UPI, Bank Transfer, or Digital Wallet. Payment status is simulated for this demo.';
    if (isBuyer) return 'I can help you find verified farmers based on crop, location, quantity, quality, and price. Try asking for farmers with a specific crop and quantity.';
    if (isDelivery) return 'Your bulk delivery route is Farm → Collection Center → Transport → Warehouse. Open Delivery to view the current mock status.';
    if (isOrder) return 'Your active bulk orders are being matched with verified farmers. Open Bulk Orders to review quantities, target prices, and status.';
    if (isPrice) return 'Bulk tomato pricing is simulated at ₹29/kg for the current requirement. Open Requirements to compare farmer offers.';
  }

  if (role === 'delivery') {
    if (isOrder || query.includes('deliveries')) return 'You have assigned deliveries available in your delivery dashboard. Open Active Delivery to view pickup location, destination, quantity, and delivery status.';
    if (isDelivery) return 'Your next demo delivery can be viewed from the Active Delivery section. It includes the pickup point, destination, route, and estimated arrival.';
    if (isHelp) return 'Open the route board for pickup instructions, recipient contact, and the current delivery timeline.';
  }

  if (role === 'volunteer') {
    if (query.includes('farmer') || query.includes('farmers')) return 'There are farmer assistance requests waiting for support. You can open Help Requests to view their location, issue, phone number, and request time.';
    if (isHelp || query.includes('support')) return 'You can accept an assistance request and help the farmer with crop listing, price checking, ordering, payments, or delivery tracking.';
    if (isOrder) return 'Open Help Requests to find farmers who need support with placing or tracking an order.';
  }

  if (query.includes('rating')) return 'Ratings help the network build trust. You can review product quality, communication, price, delivery, and overall experience.';
  if (query.includes('quality')) return 'Quality records include grade, freshness, size, color, moisture, and verification status. All quality values in this prototype are simulated.';
  if (query.includes('reward')) return 'Rewards are simulated points for useful actions such as listing crops, completing orders, rating partners, and repeat purchases.';
  return 'I can help with crop prices, demand, price prediction, orders, payments, delivery tracking, ratings, rewards, or volunteer support. Please ask me about one of these.';
}

function Chat({ role, onClose, onToast }: { role: Role; onClose: () => void; onToast: (s: string) => void }) {
  const [messages, setMessages] = useState([{ from: 'bot', text: 'Hello. I can help with crop prices, buyers, payments, or delivery.' }]);
  const [input, setInput] = useState('');
  const [listening, setListening] = useState(false);
  const quick = ['Track Order', 'Payment Help', 'Crop Price', 'Find Buyer', 'Request Volunteer', 'Delivery Support'];
  const send = (text = input) => { if (!text.trim()) return; setMessages(m => [...m, { from: 'you', text }, { from: 'bot', text: getChatResponse(text, role) }]); setInput(''); };
  const mic = () => { const Speech = (window as unknown as { webkitSpeechRecognition?: new () => { start: () => void; onresult: (e: { results: { 0: { 0: { transcript: string } } } }) => void; onend: () => void } }).webkitSpeechRecognition; if (Speech) { const recognition = new Speech(); recognition.onresult = e => setInput(e.results[0][0].transcript); recognition.onend = () => setListening(false); recognition.start(); setListening(true); } else { setListening(true); onToast('Microphone simulation active'); window.setTimeout(() => { setInput('Show tomato prices'); setListening(false); }, 900); } };
  return <div className="fixed bottom-20 right-4 z-50 flex w-[min(380px,calc(100vw-2rem))] flex-col overflow-hidden rounded-3xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] shadow-2xl fade-up" data-testid="panel-agribot"><div className="flex items-center justify-between bg-[hsl(var(--primary))] px-5 py-4 text-[hsl(var(--primary-foreground))]"><div className="flex items-center gap-3"><div className="grid h-9 w-9 place-items-center rounded-full bg-[hsl(var(--accent))] text-[hsl(var(--primary))]"><Bot size={18} /></div><div><p className="font-bold">AgriBot</p><p className="text-[11px] text-[hsl(var(--primary-foreground)/.65)]">Demo assistant • online</p></div></div><button onClick={onClose} data-testid="button-close-agribot"><X size={18} /></button></div><div className="max-h-72 space-y-3 overflow-auto p-4">{messages.map((m, i) => <div key={i} className={`max-w-[85%] rounded-2xl px-3 py-2 text-sm ${m.from === 'you' ? 'ml-auto bg-[hsl(var(--accent)/.35)]' : 'bg-[hsl(var(--muted))]'}`} data-testid={`text-chat-${i}`}>{m.text}</div>)}</div><div className="flex flex-wrap gap-1.5 border-t border-[hsl(var(--border))] px-4 py-3">{quick.map(q => <button key={q} onClick={() => { send(q); if (q === 'Request Volunteer') onToast('Volunteer request drafted'); }} className="rounded-full border border-[hsl(var(--border))] px-2.5 py-1 text-[10px] font-semibold hover:border-[hsl(var(--primary))]" data-testid={`button-quick-${q.toLowerCase().replaceAll(' ', '-')}`}>{q}</button>)}</div><div className="flex items-center gap-2 border-t border-[hsl(var(--border))] p-3"><input value={input} onChange={e => setInput(e.target.value)} onKeyDown={e => e.key === 'Enter' && send()} placeholder="Ask a question..." className="min-w-0 flex-1 bg-transparent px-1 text-sm outline-none" data-testid="input-chat" /><button onClick={mic} className={`rounded-lg p-2 ${listening ? 'bg-[hsl(var(--accent))]' : ''}`} data-testid="button-chat-mic"><Mic size={17} /></button><button onClick={() => send()} className="rounded-lg bg-[hsl(var(--primary))] p-2 text-[hsl(var(--primary-foreground))]" data-testid="button-chat-send"><Send size={16} /></button></div></div>;
}

export function SectionTitle({ eyebrow, title, subtitle, action }: { eyebrow?: string; title: string; subtitle?: string; action?: React.ReactNode }) {
  return <div className="mb-7 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="mb-2 font-mono text-[10px] uppercase tracking-[.18em] text-[hsl(var(--primary))]">{eyebrow || 'Workspace'}</p><h1 className="font-display text-3xl tracking-[-.025em] sm:text-4xl">{title}</h1>{subtitle && <p className="mt-2 max-w-2xl text-sm text-[hsl(var(--muted-foreground))]">{subtitle}</p>}</div>{action}</div>;
}
export function StatCard({ label, value, note, accent = 'green', icon: Icon = ArrowUpRight }: { label: string; value: string; note?: string; accent?: string; icon?: LucideIcon }) { return <div className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-4 shadow-sm transition-transform hover:-translate-y-0.5 sm:p-5"><div className="mb-5 flex items-center justify-between"><span className="text-xs font-semibold text-[hsl(var(--muted-foreground))]">{label}</span><span className={`grid h-8 w-8 place-items-center rounded-xl ${accent === 'amber' ? 'bg-[hsl(var(--accent)/.35)] text-[hsl(var(--primary))]' : accent === 'blue' ? 'bg-[#d9edf0] text-[#176c72]' : 'bg-[#dcebd9] text-[hsl(var(--primary))]'}`}><Icon size={16} /></span></div><p className="font-display text-2xl">{value}</p>{note && <p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">{note}</p>}</div>; }
export function Status({ children, tone = 'green' }: { children: React.ReactNode; tone?: 'green' | 'amber' | 'blue' | 'red' }) { const styles = { green: 'bg-[#dcebd9] text-[#2c6738]', amber: 'bg-[#f5e8bc] text-[#7e5d13]', blue: 'bg-[#d9edf0] text-[#176c72]', red: 'bg-[#f3d4ce] text-[#9b3f32]' }; return <span className={`inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-[11px] font-bold ${styles[tone]}`}><span className="h-1.5 w-1.5 rounded-full bg-current" />{children}</span>; }

export function Dashboard({ role }: { role: Role }) {
  const [location, setLocation] = useState('Guntur');
  const [orderStatus, setOrderStatus] = useState('In Transit');
  const [cart, setCart] = useState<string[]>([]);
  const [toast, setToast] = useState('');
  const show = (m: string) => { setToast(m); window.setTimeout(() => setToast(''), 2200); };
  if (role === 'consumer') return <ConsumerDashboard cart={cart} setCart={setCart} show={show} toast={toast} />;
  if (role === 'bulk') return <BulkDashboard show={show} toast={toast} />;
  if (role === 'delivery') return <DeliveryDashboard show={show} toast={toast} />;
  if (role === 'volunteer') return <VolunteerDashboard show={show} toast={toast} />;
  const demand = demandByLocation[location];
  return <div className="fade-up"><SectionTitle eyebrow="Farmer workspace / Tuesday, 03 Sept" title="Good morning, Lakshya." subtitle="Your farm-to-market picture at a glance. The decisions below are grounded in your produce and your buyers." action={<button onClick={() => show('Sell form opened')} className="inline-flex items-center justify-center gap-2 rounded-xl bg-[hsl(var(--primary))] px-4 py-2.5 text-sm font-bold text-[hsl(var(--primary-foreground))]" data-testid="button-sell-produce"><Plus size={17} /> Sell produce</button>} /><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-6"><StatCard label="Active orders" value="8" note="+2 this week" /><StatCard label="Delivered" value="42" note="96% on-time" /><StatCard label="Pending" value="3" note="Needs your attention" accent="amber" /><StatCard label="Total earnings" value="₹1,84,500" note="+14.8% this season" /><StatCard label="Pending payments" value="₹32,000" note="2 advances due" accent="amber" icon={WalletCards} /><StatCard label="Available crops" value="4" note="Ready to sell" icon={Wheat} /></div><div className="mt-5 grid gap-5 xl:grid-cols-[1.35fr_.65fr]"><div className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5 sm:p-6"><div className="mb-5 flex items-start justify-between"><div><p className="font-mono text-[10px] uppercase tracking-[.15em] text-[hsl(var(--muted-foreground))]">Most important today</p><h2 className="mt-1 font-display text-2xl">Order #AG1024</h2></div><Status tone="amber">{orderStatus}</Status></div><div className="grid gap-5 sm:grid-cols-[1fr_1.2fr]"><div className="flex gap-4"><div className="grid h-16 w-16 place-items-center rounded-2xl bg-[#efb3a3] font-display text-2xl text-[#843f34]">T</div><div><p className="font-bold">Fresh tomato</p><p className="text-sm text-[hsl(var(--muted-foreground))]">FreshMart Foods • 800 kg</p><p className="mt-2 font-mono text-sm">₹30/kg <span className="text-[hsl(var(--muted-foreground))]">• ₹24,000 total</span></p></div></div><div><div className="mb-2 flex justify-between text-xs"><span>Payment progress</span><b>40% advance</b></div><div className="h-2 overflow-hidden rounded-full bg-[hsl(var(--muted))]"><div className="h-full w-[40%] rounded-full bg-[hsl(var(--accent))]" /></div><div className="mt-5 flex items-center justify-between text-xs text-[hsl(var(--muted-foreground))]"><span>Expected 8 Sept</span><button onClick={() => { setOrderStatus('Near destination'); show('Order status advanced'); }} className="font-bold text-[hsl(var(--primary))]" data-testid="button-advance-order">Advance status <ArrowUpRight size={14} className="inline" /></button></div></div></div><div className="mt-6 grid grid-cols-4 gap-1">{['Confirmed', 'Packed', 'In Transit', 'Delivered'].map((step, i) => <div key={step}><div className={`mb-2 h-1 rounded-full ${i < 2 || (i === 2 && orderStatus !== 'In Transit') ? 'bg-[hsl(var(--primary))]' : 'bg-[hsl(var(--muted))]'}`} /><p className="text-[10px] text-[hsl(var(--muted-foreground))]">{step}</p></div>)}</div></div><div className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--primary))] p-5 text-[hsl(var(--primary-foreground))] sm:p-6"><div className="flex items-center justify-between"><div><p className="font-mono text-[10px] uppercase tracking-[.15em] text-[hsl(var(--accent))]">Local demand pulse</p><h2 className="mt-1 font-display text-2xl">{location}</h2></div><select value={location} onChange={e => setLocation(e.target.value)} className="rounded-lg border border-[hsl(var(--sidebar-border))] bg-[hsl(var(--sidebar-accent))] px-2 py-1.5 text-xs text-[hsl(var(--sidebar-foreground))]" data-testid="select-demand-location">{Object.keys(demandByLocation).map(l => <option key={l}>{l}</option>)}</select></div><p className="mt-2 text-xs text-[hsl(var(--primary-foreground)/.65)]">{demand.market} • mock signal updated 2 hrs ago</p><div className="mt-7 space-y-4">{[['High demand', demand.high, 'bg-[hsl(var(--accent))]'], ['Medium', demand.medium, 'bg-[#89b5a0]'], ['Low', demand.low, 'bg-[#b6c2a3]']].map(([label, items, color]) => <div key={String(label)} className="flex items-start gap-3"><span className={`mt-1 h-2 w-2 shrink-0 rounded-full ${color}`} /><div><p className="text-xs font-bold">{label as string}</p><p className="mt-1 text-sm text-[hsl(var(--primary-foreground)/.72)]">{(items as string[]).join(' / ')}</p></div></div>)}</div><p className="mt-7 rounded-xl border border-[hsl(var(--sidebar-border))] bg-[hsl(var(--sidebar-accent))] p-3 text-[11px] leading-4 text-[hsl(var(--primary-foreground)/.7)]"><Sparkles size={13} className="mr-1 inline text-[hsl(var(--accent))]" /> AI market signal • Demo data, not a guaranteed forecast.</p></div></div><div className="mt-5 grid gap-5 lg:grid-cols-[1fr_.8fr]"><PriceChart /><CropSnapshot onSell={() => show('Selling flow started')} /></div><Toast message={toast} /></div>;
}

function PriceChart() { const [crop, setCrop] = useState<'Tomato' | 'Rice' | 'Chilli'>('Tomato'); return <div className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5 sm:p-6"><div className="mb-4 flex flex-wrap items-start justify-between gap-3"><div><p className="font-mono text-[10px] uppercase tracking-[.15em] text-[hsl(var(--muted-foreground))]">Market intelligence</p><h2 className="mt-1 font-display text-2xl">Crop price history</h2></div><div className="flex rounded-lg bg-[hsl(var(--muted))] p-1">{(['Tomato', 'Rice', 'Chilli'] as const).map(c => <button key={c} onClick={() => setCrop(c)} className={`rounded-md px-2 py-1 text-[10px] font-bold ${crop === c ? 'bg-[hsl(var(--card))] text-[hsl(var(--primary))] shadow-sm' : 'text-[hsl(var(--muted-foreground))]'}`} data-testid={`button-chart-${c.toLowerCase()}`}>{c}</button>)}</div></div><div className="h-52 w-full"><ResponsiveContainer width="100%" height="100%"><AreaChart data={priceHistory}><defs><linearGradient id="priceFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#487c59" stopOpacity={.28} /><stop offset="100%" stopColor="#487c59" stopOpacity={0} /></linearGradient></defs><CartesianGrid strokeDasharray="3 3" stroke="#dfe5d9" vertical={false} /><XAxis dataKey="month" tick={{ fontSize: 11 }} axisLine={false} tickLine={false} /><YAxis tick={{ fontSize: 10 }} axisLine={false} tickLine={false} width={26} /><Tooltip contentStyle={{ borderRadius: 12, border: '1px solid #dfe5d9', fontSize: 12 }} /><Area type="monotone" dataKey={crop} stroke="#487c59" fill="url(#priceFill)" strokeWidth={3} /></AreaChart></ResponsiveContainer></div><p className="mt-2 text-[10px] text-[hsl(var(--muted-foreground))]">₹/kg • historical mock data through September</p></div>; }
function CropSnapshot({ onSell }: { onSell: () => void }) { return <div className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5 sm:p-6"><div className="mb-4 flex items-center justify-between"><div><p className="font-mono text-[10px] uppercase tracking-[.15em] text-[hsl(var(--muted-foreground))]">Ready in your shed</p><h2 className="mt-1 font-display text-2xl">Crop snapshot</h2></div><Link href="/farmer/crops" className="text-xs font-bold text-[hsl(var(--primary))]" data-testid="link-all-crops">View all</Link></div><div className="space-y-2.5">{crops.slice(0, 4).map(c => <div key={c.id} className="flex items-center gap-3 rounded-xl border border-[hsl(var(--border))] p-2.5"><span className={`grid h-9 w-9 place-items-center rounded-lg ${c.color} font-display text-[#4d5140]`}>{c.emoji}</span><div className="min-w-0 flex-1"><p className="text-sm font-bold">{c.name}</p><p className="text-[11px] text-[hsl(var(--muted-foreground))]">{c.quantity} available</p></div><p className="font-mono text-xs font-bold">{c.price}</p></div>)}</div><button onClick={onSell} className="mt-4 w-full rounded-xl border border-[hsl(var(--primary))] py-2 text-xs font-bold text-[hsl(var(--primary))] hover:bg-[hsl(var(--secondary))]" data-testid="button-snapshot-sell">List a crop for sale</button></div>; }

function ConsumerDashboard({ cart, setCart, show, toast }: { cart: string[]; setCart: React.Dispatch<React.SetStateAction<string[]>>; show: (s: string) => void; toast: string }) { return <div className="fade-up"><SectionTitle eyebrow="Priya's kitchen / Tuesday, 03 Sept" title="Good food, closer to home." subtitle="Shop produce with a clear trail back to the people who grew it." action={<Link href="/consumer/shop" className="inline-flex items-center justify-center gap-2 rounded-xl bg-[hsl(var(--primary))] px-4 py-2.5 text-sm font-bold text-[hsl(var(--primary-foreground))]" data-testid="link-start-shopping"><ShoppingBasket size={17} /> Shop fresh</Link>} /><div className="grid gap-3 sm:grid-cols-3"><StatCard label="Active orders" value="2" note="Both arrive this week" /><StatCard label="Saved this month" value="₹486" note="Direct-from-farm value" accent="amber" icon={PiggyBank} /><StatCard label="Reward points" value="1,240" note="460 to next harvest" icon={Medal} /></div><div className="mt-5 grid gap-5 xl:grid-cols-[1.2fr_.8fr]"><div className="rounded-2xl bg-[hsl(var(--primary))] p-6 text-[hsl(var(--primary-foreground))]"><div className="flex items-center justify-between"><div><p className="font-mono text-[10px] uppercase tracking-[.15em] text-[hsl(var(--accent))]">This week's table</p><h2 className="mt-2 font-display text-3xl">Harvest, not shelves.</h2><p className="mt-2 max-w-sm text-sm leading-6 text-[hsl(var(--primary-foreground)/.7)]">Four local picks are at their freshest today. Each order supports a named farm.</p></div><div className="hidden h-28 w-28 rotate-3 rounded-[2rem] bg-[#efb3a3] p-4 font-display text-5xl text-[#843f34] sm:block">T</div></div><div className="mt-7 grid grid-cols-2 gap-2 sm:grid-cols-4">{products.map(p => <button key={p.id} onClick={() => { setCart(c => [...c, p.id]); show(`${p.name} added to basket`); }} className="rounded-xl border border-[hsl(var(--sidebar-border))] bg-[hsl(var(--sidebar-accent))] p-3 text-left hover:-translate-y-0.5" data-testid={`button-add-dashboard-${p.id}`}><div className={`mb-3 grid h-12 place-items-center rounded-lg ${p.color} font-display text-xl text-[#4d5140]`}>{p.name[0]}</div><p className="truncate text-xs font-bold">{p.name}</p><p className="mt-1 font-mono text-[10px] text-[hsl(var(--primary-foreground)/.65)]">{money(p.price)}</p></button>)}</div></div><div className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-6"><div className="mb-5 flex items-center justify-between"><div><p className="font-mono text-[10px] uppercase tracking-[.15em] text-[hsl(var(--muted-foreground))]">Latest order</p><h2 className="mt-1 font-display text-2xl">#PR4921</h2></div><Status>Confirmed</Status></div><div className="flex gap-3"><div className="grid h-12 w-12 place-items-center rounded-xl bg-[#e8d58f] font-display text-xl">R</div><div><p className="text-sm font-bold">Sona Masuri Rice</p><p className="text-xs text-[hsl(var(--muted-foreground))]">5 kg • from Savitri Organics</p><p className="mt-2 font-mono text-sm">₹210</p></div></div><div className="mt-7 space-y-3">{['Order confirmed', 'Packed at farm', 'Out for delivery'].map((s, i) => <div className="flex items-center gap-3" key={s}><span className={`grid h-6 w-6 place-items-center rounded-full ${i < 2 ? 'bg-[hsl(var(--primary))] text-[hsl(var(--primary-foreground))]' : 'bg-[hsl(var(--muted))] text-[hsl(var(--muted-foreground))]'}`}>{i < 2 ? <Check size={13} /> : <span className="h-1.5 w-1.5 rounded-full bg-current" />}</span><span className="text-xs">{s}</span></div>)}</div><Link href="/consumer/delivery" className="mt-7 block text-center text-xs font-bold text-[hsl(var(--primary))]" data-testid="link-track-consumer-order">Track delivery <ArrowUpRight size={14} className="inline" /></Link></div></div>{cart.length > 0 && <div className="fixed bottom-5 left-5 z-30 rounded-full border border-[hsl(var(--border))] bg-[hsl(var(--card))] px-4 py-3 text-sm font-bold shadow-lg">{cart.length} item{cart.length > 1 ? 's' : ''} in basket <Link href="/consumer/shop" className="ml-2 text-[hsl(var(--primary))]" data-testid="link-cart">View cart</Link></div>}<Toast message={toast} /></div>; }

function BulkDashboard({ show, toast }: { show: (s: string) => void; toast: string }) { return <div className="fade-up"><SectionTitle eyebrow="Aruna Foods / Procurement desk" title="Buy with confidence." subtitle="Your weekly procurement board, from requirement to receiving bay." action={<Link href="/bulk/requirements" className="rounded-xl bg-[hsl(var(--primary))] px-4 py-2.5 text-sm font-bold text-[hsl(var(--primary-foreground))]" data-testid="link-create-requirement">Create requirement</Link>} /><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><StatCard label="Open requirements" value="6" note="2 need matching" /><StatCard label="Active procurement" value="₹4,80,000" note="Across 4 farmers" /><StatCard label="In delivery" value="3" note="1 arriving today" accent="amber" icon={Truck} /><StatCard label="Direct-farm savings" value="₹38,420" note="This quarter" icon={PiggyBank} /></div><div className="mt-5 grid gap-5 xl:grid-cols-[1fr_.8fr]"><div className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-6"><div className="flex items-center justify-between"><div><p className="font-mono text-[10px] uppercase tracking-[.15em] text-[hsl(var(--muted-foreground))]">Priority requirement</p><h2 className="mt-1 font-display text-2xl">Tomato / 4,000 kg</h2></div><Status tone="amber">Matching farmers</Status></div><p className="mt-2 text-sm text-[hsl(var(--muted-foreground))]">Needed by 12 Sept • Grade A • delivery to Hyderabad</p><div className="mt-6 flex items-center gap-3"><div className="flex -space-x-2"><span className="grid h-9 w-9 place-items-center rounded-full border-2 border-[hsl(var(--card))] bg-[#dcebd9] font-mono text-[10px]">LK</span><span className="grid h-9 w-9 place-items-center rounded-full border-2 border-[hsl(var(--card))] bg-[#e8d58f] font-mono text-[10px]">SN</span><span className="grid h-9 w-9 place-items-center rounded-full border-2 border-[hsl(var(--card))] bg-[#d9edf0] font-mono text-[10px]">+4</span></div><span className="text-xs text-[hsl(var(--muted-foreground))]">6 verified farmers found</span></div><Link href="/bulk/farmers" className="mt-6 inline-flex items-center gap-2 rounded-xl bg-[hsl(var(--secondary))] px-4 py-2.5 text-xs font-bold text-[hsl(var(--primary))]" data-testid="link-view-farmers">Review matches <ArrowUpRight size={14} /></Link></div><div className="rounded-2xl bg-[hsl(var(--primary))] p-6 text-[hsl(var(--primary-foreground))]"><p className="font-mono text-[10px] uppercase tracking-[.15em] text-[hsl(var(--accent))]">Receiving forecast</p><h2 className="mt-1 font-display text-2xl">On track this week</h2><div className="mt-7 h-40"><ResponsiveContainer width="100%" height="100%"><BarChart data={[{ d: 'Mon', v: 8 }, { d: 'Tue', v: 12 }, { d: 'Wed', v: 9 }, { d: 'Thu', v: 17 }, { d: 'Fri', v: 13 }]}><XAxis dataKey="d" tick={{ fill: '#dce8db', fontSize: 10 }} axisLine={false} tickLine={false} /><Bar dataKey="v" fill="#f1cc68" radius={[5, 5, 0, 0]} /></BarChart></ResponsiveContainer></div><p className="text-xs text-[hsl(var(--primary-foreground)/.65)]">Crates received • simulated operations data</p></div></div><Toast message={toast} /></div>; }

function DeliveryDashboard({ show, toast }: { show: (s: string) => void; toast: string }) { const [status, setStatus] = useState('Picked Up'); const next = ({ Assigned: 'Picked Up', 'Picked Up': 'In Transit', 'In Transit': 'Near Destination', 'Near Destination': 'Delivered', Delivered: 'Delivered' } as Record<string, string>)[status] || 'Delivered'; return <div className="fade-up"><SectionTitle eyebrow="Ravi's route board / 03 Sept" title="Your route is ready." subtitle="Keep the chain visible. Advance each pickup as it moves from farm gate to receiving bay." action={<Status tone="blue">Online • accepting routes</Status>} /><div className="grid gap-3 sm:grid-cols-4"><StatCard label="Assigned today" value="4" note="12 stops total" /><StatCard label="Active" value="2" note="Next: 14 min away" accent="amber" icon={Navigation} /><StatCard label="Completed" value="18" note="This month" /><StatCard label="Pending pickups" value="2" note="Before 3:00 PM" accent="amber" icon={Clock3} /></div><div className="mt-5 grid gap-5 xl:grid-cols-[1fr_.72fr]"><div className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5 sm:p-6"><div className="flex items-start justify-between"><div><p className="font-mono text-[10px] uppercase tracking-[.15em] text-[hsl(var(--muted-foreground))]">Active delivery / route 02</p><h2 className="mt-1 font-display text-2xl">#AG1024 • Tomato</h2></div><Status tone={status === 'Delivered' ? 'green' : 'amber'}>{status}</Status></div><div className="relative mt-8 space-y-6 pl-7">{['Assigned', 'Picked Up', 'In Transit', 'Near Destination', 'Delivered'].map((s, i) => <div key={s} className="relative flex items-center gap-3"><span className={`absolute -left-7 grid h-5 w-5 place-items-center rounded-full border-2 ${['Assigned', 'Picked Up', 'In Transit', 'Near Destination', 'Delivered'].indexOf(status) >= i ? 'border-[hsl(var(--primary))] bg-[hsl(var(--primary))] text-white' : 'border-[hsl(var(--border))] bg-[hsl(var(--card))]'}`}>{['Assigned', 'Picked Up', 'In Transit', 'Near Destination', 'Delivered'].indexOf(status) >= i && <Check size={11} />}</span><span className={status === s ? 'font-bold' : 'text-sm text-[hsl(var(--muted-foreground))]'}>{s}</span><span className="ml-auto text-[11px] text-[hsl(var(--muted-foreground))]">{i === 0 ? '08:10' : i === 1 ? '08:42' : i === 2 ? '09:10' : i === 3 ? '09:28' : '—'}</span></div>)}<div className="absolute left-[-17px] top-3 h-[calc(100%-20px)] w-px bg-[hsl(var(--border))]" /></div><div className="mt-8 flex flex-wrap gap-2"><button onClick={() => { setStatus(next); show(`Status updated to ${next}`); }} disabled={status === 'Delivered'} className="rounded-xl bg-[hsl(var(--primary))] px-4 py-2.5 text-xs font-bold text-[hsl(var(--primary-foreground))] disabled:opacity-50" data-testid="button-advance-delivery">{status === 'Delivered' ? 'Route completed' : `Mark ${next}`}</button><button onClick={() => show('Recipient contact opened')} className="rounded-xl border border-[hsl(var(--border))] px-4 py-2.5 text-xs font-bold" data-testid="button-call-recipient"><Phone size={14} className="mr-1 inline" /> Call recipient</button></div></div><div className="agri-grid min-h-[320px] rounded-2xl border border-[hsl(var(--border))] bg-[#edf1e7] p-5"><div className="flex items-center justify-between"><p className="font-mono text-[10px] uppercase tracking-[.15em] text-[hsl(var(--muted-foreground))]">Live route view</p><Status tone="blue">Updated 2 min ago</Status></div><div className="relative mt-5 h-56"><div className="absolute left-[18%] top-[28%] h-4 w-4 rounded-full border-4 border-[#edf1e7] bg-[hsl(var(--primary))] shadow-[0_0_0_5px_hsl(var(--primary)/.18)]" /><div className="absolute right-[20%] top-[66%] h-4 w-4 rounded-full border-4 border-[#edf1e7] bg-[hsl(var(--accent))] shadow-[0_0_0_5px_hsl(var(--accent)/.3)]" /><div className="absolute left-[20%] top-[34%] h-28 w-[55%] rotate-[21deg] border-b-2 border-dashed border-[hsl(var(--primary))]" /><div className="absolute left-[10%] top-[16%] text-[10px] font-bold">Lakshya Farms</div><div className="absolute bottom-[13%] right-[8%] text-[10px] font-bold">FreshMart receiving</div></div><div className="grid grid-cols-3 gap-2 border-t border-[#d2dccc] pt-4 text-center"><div><p className="font-mono text-sm font-bold">18.4 km</p><p className="text-[10px] text-[hsl(var(--muted-foreground))]">distance</p></div><div><p className="font-mono text-sm font-bold">42 min</p><p className="text-[10px] text-[hsl(var(--muted-foreground))]">ETA</p></div><div><p className="font-mono text-sm font-bold">12°C</p><p className="text-[10px] text-[hsl(var(--muted-foreground))]">cold chain</p></div></div></div></div><Toast message={toast} /></div>; }

function VolunteerDashboard({ show, toast }: { show: (s: string) => void; toast: string }) { const [requests, setRequests] = useState(['rq1', 'rq2']); return <div className="fade-up"><SectionTitle eyebrow="Meera's support desk / Today" title="Small help, real momentum." subtitle="Farmers ask for practical help. You bring the patient conversation." action={<Status tone="green">Available for calls</Status>} /><div className="grid gap-3 sm:grid-cols-4"><StatCard label="Need help now" value="7" note="3 near Guntur" /><StatCard label="Pending calls" value="3" note="Oldest: 18 min" accent="amber" icon={Phone} /><StatCard label="Completed this month" value="26" note="4.8 average" /><StatCard label="Active assistance" value="2" note="You are helping 1" icon={HandHelping} /></div><div className="mt-5 grid gap-5 lg:grid-cols-[1.1fr_.9fr]"><div className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5 sm:p-6"><div className="mb-5 flex items-center justify-between"><div><p className="font-mono text-[10px] uppercase tracking-[.15em] text-[hsl(var(--muted-foreground))]">Request queue</p><h2 className="mt-1 font-display text-2xl">Farmers needing you</h2></div><Link href="/volunteer/requests" className="text-xs font-bold text-[hsl(var(--primary))]" data-testid="link-all-requests">View all</Link></div>{[['rq1', 'Lakshya', 'Needs help listing 800 kg tomato', 'Guntur • 6 min ago'], ['rq2', 'Savitri Rao', 'Cannot confirm payment for rice order', 'Tenali • 18 min ago']].map(([id, name, issue, place]) => requests.includes(id) && <div key={id} className="mb-3 rounded-xl border border-[hsl(var(--border))] p-4"><div className="flex items-start gap-3"><div className="grid h-10 w-10 place-items-center rounded-full bg-[hsl(var(--secondary))] font-mono text-xs font-bold">{String(name).split(' ').map(v => v[0]).join('')}</div><div className="min-w-0 flex-1"><div className="flex justify-between gap-3"><p className="text-sm font-bold">{name}</p><Status tone="amber">Pending</Status></div><p className="mt-1 text-sm">{issue}</p><p className="mt-1 text-[11px] text-[hsl(var(--muted-foreground))]">{place}</p></div></div><div className="mt-4 flex gap-2"><button onClick={() => show(`Calling ${name}`)} className="rounded-lg bg-[hsl(var(--primary))] px-3 py-2 text-[11px] font-bold text-[hsl(var(--primary-foreground))]" data-testid={`button-call-${id}`}><Phone size={13} className="mr-1 inline" /> Call farmer</button><button onClick={() => { setRequests(r => r.filter(x => x !== id)); show('Request marked completed'); }} className="rounded-lg border border-[hsl(var(--border))] px-3 py-2 text-[11px] font-bold" data-testid={`button-complete-${id}`}>Mark completed</button></div></div>)}</div><div className="rounded-2xl bg-[hsl(var(--primary))] p-6 text-[hsl(var(--primary-foreground))]"><p className="font-mono text-[10px] uppercase tracking-[.15em] text-[hsl(var(--accent))]">Your impact</p><h2 className="mt-1 font-display text-3xl">₹2.4L unlocked</h2><p className="mt-2 text-sm leading-6 text-[hsl(var(--primary-foreground)/.7)]">Estimated value of orders supported through your calls. Simulated community metric.</p><div className="mt-8 border-t border-[hsl(var(--sidebar-border))] pt-5"><div className="flex justify-between text-xs"><span>Monthly goal</span><span>26 / 30 calls</span></div><div className="mt-2 h-2 rounded-full bg-[hsl(var(--sidebar-accent))]"><div className="h-full w-[86%] rounded-full bg-[hsl(var(--accent))]" /></div></div><button onClick={() => show('Next available request loaded')} className="mt-7 flex items-center gap-2 text-xs font-bold text-[hsl(var(--accent))]" data-testid="button-next-request">Take next request <ArrowUpRight size={14} /></button></div></div><Toast message={toast} /></div>; }

export function Toast({ message }: { message: string }) { return message ? <div className="fixed bottom-5 left-1/2 z-50 -translate-x-1/2 rounded-full bg-[hsl(var(--primary))] px-5 py-3 text-sm font-semibold text-[hsl(var(--primary-foreground))] shadow-lg fade-up" data-testid="toast-global">{message}</div> : null; }