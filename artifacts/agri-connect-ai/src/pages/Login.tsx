import { type FormEvent, useState } from 'react';
import { Link, useLocation } from 'wouter';
import { ArrowRight, BarChart3, Check, CircleUserRound, Eye, EyeOff, HandHelping, Leaf, ShieldCheck, ShoppingBasket, Truck } from 'lucide-react';
import { roleInfo, type Role } from '@/data/mockData';

type LoginRole = Exclude<Role, 'bulk'>;

const loginRoles: { id: LoginRole; title: string; icon: typeof Leaf }[] = [
  { id: 'farmer', title: 'Farmer', icon: Leaf },
  { id: 'consumer', title: 'Consumer', icon: ShoppingBasket },
  { id: 'delivery', title: 'Delivery Person', icon: Truck },
  { id: 'volunteer', title: 'Volunteer', icon: HandHelping },
];

export function Login() {
  const [, setLocation] = useLocation();
  const [selected, setSelected] = useState<LoginRole>('farmer');
  const [consumerType, setConsumerType] = useState<'consumer' | 'bulk'>('consumer');
  const [identifier, setIdentifier] = useState('');
  const [password, setPassword] = useState('');
  const [showPassword, setShowPassword] = useState(false);
  const [rememberMe, setRememberMe] = useState(false);
  const [errors, setErrors] = useState<{ identifier?: string; password?: string }>({});
  const [notice, setNotice] = useState('');

  function handleLogin(event: FormEvent<HTMLFormElement>) {
    event.preventDefault();
    const nextErrors: { identifier?: string; password?: string } = {};
    if (!identifier.trim()) nextErrors.identifier = 'Please enter your email or mobile number.';
    if (!password.trim()) nextErrors.password = 'Please enter your password.';
    setErrors(nextErrors);
    if (Object.keys(nextErrors).length > 0) return;
    setLocation(`/${selected === 'consumer' ? consumerType : selected}/dashboard`);
  }

  return <div className="min-h-[100dvh] bg-[hsl(var(--background))]"><header className="mx-auto max-w-6xl px-5 py-6 sm:px-8"><Link href="/" className="inline-flex" data-testid="link-login-brand"><span className="grid h-9 w-9 place-items-center rounded-xl bg-[hsl(var(--accent))] font-display text-lg text-[hsl(var(--primary))]">A</span></Link></header><main className="mx-auto grid max-w-6xl gap-12 px-5 pb-16 pt-8 sm:px-8 lg:grid-cols-[.8fr_1.2fr] lg:items-center lg:pt-12"><section><p className="font-mono text-[10px] uppercase tracking-[.18em] text-[hsl(var(--primary))]">Secure sign in</p><h1 className="mt-3 font-display text-5xl leading-[.98] tracking-[-.04em]">Welcome to AgriConnect AI</h1><p className="mt-5 max-w-md text-sm leading-6 text-[hsl(var(--muted-foreground))]">Sign in to open the workspace for your role. Your dashboard, market insights, and support tools are ready when you are.</p><div className="mt-8 flex items-start gap-3 rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-4"><ShieldCheck className="mt-0.5 text-[hsl(var(--primary))]" size={19} /><p className="text-xs leading-5 text-[hsl(var(--muted-foreground))]"><b className="text-[hsl(var(--foreground))]">Safe for demos.</b> Market values, AI insights, and payment confirmations are clearly simulated.</p></div></section><section className="rounded-3xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5 shadow-sm sm:p-7"><form onSubmit={handleLogin} noValidate><div className="mb-5 flex items-center justify-between"><div><p className="font-display text-2xl">Login</p><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">Choose your role and enter your details</p></div><CircleUserRound className="text-[hsl(var(--muted-foreground))]" /></div><fieldset><legend className="mb-3 text-sm font-bold">Login as:</legend><div className="grid gap-2 sm:grid-cols-2">{loginRoles.map(role => { const Icon = role.icon; const active = selected === role.id; return <button type="button" key={role.id} onClick={() => { setSelected(role.id); setNotice(''); }} className={`flex items-center gap-3 rounded-xl border p-3 text-left ${active ? 'border-[hsl(var(--primary))] bg-[hsl(var(--secondary))]' : 'border-[hsl(var(--border))]'}`} data-testid={`button-select-${role.id}`}><span className={`grid h-9 w-9 place-items-center rounded-lg ${active ? 'bg-[hsl(var(--primary))] text-[hsl(var(--primary-foreground))]' : 'bg-[hsl(var(--muted))] text-[hsl(var(--muted-foreground))]'}`}><Icon size={17} /></span><span className="min-w-0"><span className="block text-sm font-bold">{role.title}</span><span className="block truncate text-[11px] text-[hsl(var(--muted-foreground))]">{roleInfo[role.id].subtitle}</span></span>{active && <Check className="ml-auto text-[hsl(var(--primary))]" size={16} />}</button>; })}</div></fieldset>{selected === 'consumer' && <fieldset className="mt-5"><legend className="mb-3 text-sm font-bold">Consumer type:</legend><div className="grid gap-2 sm:grid-cols-2">{([{ id: 'consumer', label: 'Individual Consumer', description: 'Shop fresh produce for home' }, { id: 'bulk', label: 'Bulk Consumer', description: 'Source larger quantities' }] as const).map(option => { const active = consumerType === option.id; return <button type="button" key={option.id} onClick={() => setConsumerType(option.id)} className={`rounded-xl border p-3 text-left ${active ? 'border-[hsl(var(--primary))] bg-[hsl(var(--secondary))]' : 'border-[hsl(var(--border))]'}`} data-testid={`button-consumer-type-${option.id}`}><span className="block text-sm font-bold">{option.label}</span><span className="mt-1 block text-[11px] text-[hsl(var(--muted-foreground))]">{option.description}</span></button>; })}</div></fieldset>}<div className="mt-5 space-y-4"><div><label htmlFor="login-identifier" className="mb-2 block text-sm font-bold">Email / Mobile Number</label><input id="login-identifier" value={identifier} onChange={event => { setIdentifier(event.target.value); if (errors.identifier) setErrors(current => ({ ...current, identifier: undefined })); }} autoComplete="username" placeholder="Enter your email or mobile number" className={`w-full rounded-xl border bg-[hsl(var(--background))] px-4 py-3 text-sm outline-none transition focus:border-[hsl(var(--primary))] focus:ring-2 focus:ring-[hsl(var(--primary)/.15)] ${errors.identifier ? 'border-red-500' : 'border-[hsl(var(--border))]'}`} aria-invalid={Boolean(errors.identifier)} aria-describedby={errors.identifier ? 'login-identifier-error' : undefined} data-testid="input-login-identifier" />{errors.identifier && <p id="login-identifier-error" className="mt-2 text-xs font-medium text-red-600">{errors.identifier}</p>}</div><div><label htmlFor="login-password" className="mb-2 block text-sm font-bold">Password</label><div className="relative"><input id="login-password" type={showPassword ? 'text' : 'password'} value={password} onChange={event => { setPassword(event.target.value); if (errors.password) setErrors(current => ({ ...current, password: undefined })); }} autoComplete="current-password" placeholder="Enter your password" className={`w-full rounded-xl border bg-[hsl(var(--background))] px-4 py-3 pr-12 text-sm outline-none transition focus:border-[hsl(var(--primary))] focus:ring-2 focus:ring-[hsl(var(--primary)/.15)] ${errors.password ? 'border-red-500' : 'border-[hsl(var(--border))]'}`} aria-invalid={Boolean(errors.password)} aria-describedby={errors.password ? 'login-password-error' : undefined} data-testid="input-login-password" /><button type="button" onClick={() => setShowPassword(value => !value)} className="absolute right-3 top-1/2 -translate-y-1/2 rounded-md p-1.5 text-[hsl(var(--muted-foreground))] hover:bg-[hsl(var(--secondary))]" aria-label={showPassword ? 'Hide password' : 'Show password'} data-testid="button-toggle-password">{showPassword ? <EyeOff size={17} /> : <Eye size={17} />}</button></div>{errors.password && <p id="login-password-error" className="mt-2 text-xs font-medium text-red-600">{errors.password}</p>}</div></div><div className="mt-4 flex flex-wrap items-center justify-between gap-3 text-xs"><label className="inline-flex items-center gap-2"><input type="checkbox" checked={rememberMe} onChange={event => setRememberMe(event.target.checked)} className="h-4 w-4 accent-[hsl(var(--primary))]" data-testid="checkbox-remember-me" /> Remember me</label><button type="button" onClick={() => setNotice('Password reset is available in the full product.')} className="font-bold text-[hsl(var(--primary))]" data-testid="button-forgot-password">Forgot Password?</button></div>{notice && <p className="mt-3 rounded-lg bg-[hsl(var(--secondary))] px-3 py-2 text-xs text-[hsl(var(--primary))]" role="status">{notice}</p>}<button type="submit" className="mt-6 flex w-full items-center justify-center gap-2 rounded-xl bg-[hsl(var(--primary))] py-3.5 text-sm font-bold text-[hsl(var(--primary-foreground))]" data-testid="button-login">Login <ArrowRight size={17} /></button><p className="mt-4 text-center text-xs text-[hsl(var(--muted-foreground))]">Don't have an account? <button type="button" onClick={() => setNotice('Account creation is available in the full product.')} className="font-bold text-[hsl(var(--primary))]" data-testid="button-create-account">Create Account</button></p></form><p className="mt-6 text-center font-mono text-[9px] uppercase tracking-[.12em] text-[hsl(var(--muted-foreground))]">Normal login requires your role and account details</p></section></main></div>;
}