import { useState } from 'react';
import { Link, useLocation } from 'wouter';
import { AreaChart, Area, CartesianGrid, XAxis, YAxis, Tooltip, ResponsiveContainer } from 'recharts';
import { ArrowRight, BadgeCheck, Banknote, Bot, Check, CircleDollarSign, CreditCard, FileText, HandHelping, MapPin, MessageCircle, Minus, PackageCheck, Phone, Plus, Search, Send, ShieldCheck, Smartphone, Sparkles, Star, Truck, UsersRound, WalletCards } from 'lucide-react';
import { crops, demandByLocation, orders, priceHistory, products, type Role } from '@/data/mockData';
import { AgriShell, Dashboard, SectionTitle, StatCard, Status, Toast } from '@/components/agri-components';

export function RolePage({ role }: { role: Role }) {
  const [location] = useLocation();
  const [pathname, searchString] = location.split('?');
  const section = pathname.split('/')[2] || 'dashboard';
  const search = new URLSearchParams(searchString || '');
  return <AgriShell role={role}>{section === 'dashboard' ? <Dashboard role={role} /> : <RoleSection role={role} section={section} search={search} />}</AgriShell>;
}

function RoleSection({ role, section, search }: { role: Role; section: string; search: URLSearchParams }) {
  const [toast, setToast] = useState('');
  const [selectedLocation, setSelectedLocation] = useState('Guntur');
  const [cart, setCart] = useState<{ id: string; qty: number }[]>([]);
  const [paid, setPaid] = useState(false);
  const [advance, setAdvance] = useState(true);
  const show = (message: string) => { setToast(message); window.setTimeout(() => setToast(''), 2300); };
  const title = labels[role]?.[section] || section.replaceAll('-', ' ');
  const action = role === 'farmer' && section === 'sell' ? <button onClick={() => show('Crop listing saved to your mock marketplace')} className="rounded-xl bg-[hsl(var(--primary))] px-4 py-2.5 text-sm font-bold text-[hsl(var(--primary-foreground))]" data-testid="button-save-listing">Publish listing</button> : undefined;
  return <div className="fade-up"><SectionTitle eyebrow={`${roleLabel[role]} workspace`} title={title} subtitle={subtitles[section] || 'A practical, clear view for your next step.'} action={action} />{role === 'farmer' && <FarmerSection section={section} selectedLocation={selectedLocation} setSelectedLocation={setSelectedLocation} show={show} search={search} />}{role === 'consumer' && <ConsumerSection section={section} cart={cart} setCart={setCart} paid={paid} setPaid={setPaid} show={show} />}{role === 'bulk' && <BulkSection section={section} paid={paid} setPaid={setPaid} advance={advance} setAdvance={setAdvance} show={show} />}{role === 'delivery' && <DeliverySection section={section} show={show} />}{role === 'volunteer' && <VolunteerSection section={section} show={show} />}<Toast message={toast} /></div>;
}

const roleLabel: Record<Role, string> = { farmer: 'Farmer', consumer: 'Priya / shopper', bulk: 'Aruna Foods / procurement', delivery: 'Delivery partner', volunteer: 'Volunteer support' };
const labels: Record<Role, Record<string, string>> = {
  farmer: { crops: 'My crops', sell: 'Sell produce', demand: 'Demand signals', prices: 'Market prices', orders: 'Orders & fulfilment', payments: 'Payments', delivery: 'Delivery tracker', quality: 'Quality verification', ratings: 'Buyer ratings', rewards: 'Harvest rewards', assistant: 'AgriBot field desk', help: 'Help centre' },
  consumer: { shop: 'Shop fresh produce', orders: 'My orders', history: 'Purchase history', savings: 'Your savings', delivery: 'Delivery tracking', ratings: 'Rate your orders', rewards: 'Your rewards', assistant: 'Customer care & AgriBot' },
  bulk: { farmers: 'Find verified farmers', requirements: 'Procurement requirements', orders: 'Bulk orders', history: 'Order history', payments: 'Bulk payments', delivery: 'Inbound delivery', ratings: 'Rate your suppliers' },
  delivery: { active: 'Active route', history: 'Delivery history' },
  volunteer: { requests: 'Support requests', history: 'Call history' },
};
const subtitles: Record<string, string> = { crops: 'Keep an honest view of what is ready, what is growing, and what is already committed.', sell: 'Tell the market what you have. The AI suggestion is a demo estimate, not a promise.', demand: 'A local pulse for the crops moving fastest near your chosen market.', prices: 'Compare recent market movement before you set your asking price.', orders: 'Every handoff, amount, and next action in one place.', payments: 'A transparent ledger of simulated advances and settlements.', delivery: 'Map-style tracking without a map API. Location and ETA are mock values.', quality: 'Verification signals make first-time buyers more comfortable.', ratings: 'Trust gets built one specific rating at a time.', rewards: 'Useful habits in the field should come back to you.', assistant: 'Ask a practical question and get a demo response from AgriBot.', help: 'Call, chat, or request a volunteer for the next difficult step.', shop: 'Short routes from local farms to your kitchen.', history: 'A record of every direct-from-farm purchase.', savings: 'The value you kept by buying closer to the source.', farmers: 'Match your volumes with verified growers near your receiving point.', requirements: 'Make your next need clear to the farmer network.', active: 'Advance each handoff as the produce moves.', requests: 'The queue is made of real questions, not tickets.' };

function FarmerSection({ section, selectedLocation, setSelectedLocation, show, search }: { section: string; selectedLocation: string; setSelectedLocation: (s: string) => void; show: (s: string) => void; search: URLSearchParams }) {
  if (section === 'crops') return <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{crops.map(c => <div key={c.id} className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5 transition-transform hover:-translate-y-0.5" data-testid={`card-crop-${c.id}`}><div className={`mb-5 grid h-20 place-items-center rounded-2xl ${c.color} font-display text-4xl text-[#4d5140]`}>{c.emoji}</div><div className="flex items-start justify-between"><div><h2 className="font-display text-2xl">{c.name}</h2><p className="text-xs text-[hsl(var(--muted-foreground))]">{c.variety}</p></div><span className={`font-mono text-xs ${c.trend.startsWith('+') ? 'text-[hsl(var(--primary))]' : 'text-[#a15b46]'}`}>{c.trend}</span></div><div className="mt-5 grid grid-cols-2 gap-3 border-t border-[hsl(var(--border))] pt-4"><div><p className="text-[10px] uppercase text-[hsl(var(--muted-foreground))]">Available</p><p className="mt-1 font-mono text-sm">{c.quantity}</p></div><div><p className="text-[10px] uppercase text-[hsl(var(--muted-foreground))]">Market rate</p><p className="mt-1 font-mono text-sm">{c.price}</p></div></div><button onClick={() => show(`${c.name} selected for sale`)} className="mt-5 w-full rounded-xl border border-[hsl(var(--primary))] py-2 text-xs font-bold text-[hsl(var(--primary))]" data-testid={`button-sell-${c.id}`}>Sell this crop</button></div>)}</div>;
  if (section === 'sell') return <div className="grid gap-5 lg:grid-cols-[1fr_.75fr]"><div className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5 sm:p-7"><div className="grid gap-4 sm:grid-cols-2"><label className="text-xs font-bold">Crop<select className="mt-2 h-11 w-full rounded-xl border border-[hsl(var(--border))] bg-transparent px-3 text-sm" data-testid="select-sell-crop">{crops.map(c => <option key={c.id}>{c.name}</option>)}</select></label><label className="text-xs font-bold">Quantity<input defaultValue="800" type="number" className="mt-2 h-11 w-full rounded-xl border border-[hsl(var(--border))] bg-transparent px-3 text-sm" data-testid="input-sell-quantity" /></label><label className="text-xs font-bold">Asking price<input defaultValue="30" type="number" className="mt-2 h-11 w-full rounded-xl border border-[hsl(var(--border))] bg-transparent px-3 text-sm" data-testid="input-sell-price" /></label><label className="text-xs font-bold">Harvest date<input defaultValue="2024-09-03" type="date" className="mt-2 h-11 w-full rounded-xl border border-[hsl(var(--border))] bg-transparent px-3 text-sm" data-testid="input-harvest-date" /></label></div><label className="mt-4 block text-xs font-bold">Notes<textarea defaultValue="Grade A, sorted and packed at source." className="mt-2 min-h-24 w-full rounded-xl border border-[hsl(var(--border))] bg-transparent p-3 text-sm" data-testid="textarea-sell-notes" /></label><button onClick={() => show('Crop listing saved to your mock marketplace')} className="mt-5 rounded-xl bg-[hsl(var(--primary))] px-5 py-3 text-sm font-bold text-[hsl(var(--primary-foreground))]" data-testid="button-submit-sell">Save crop listing</button></div><div className="rounded-2xl bg-[hsl(var(--primary))] p-6 text-[hsl(var(--primary-foreground))]"><Sparkles className="text-[hsl(var(--accent))]" size={20} /><p className="mt-5 font-mono text-[10px] uppercase tracking-[.16em] text-[hsl(var(--accent))]">AI price suggestion</p><p className="mt-2 font-display text-4xl">₹30–34/kg</p><p className="mt-2 text-sm leading-6 text-[hsl(var(--primary-foreground)/.7)]">Based on the Guntur signal, recent tomato history, and your Grade A note.</p><div className="mt-7 rounded-xl border border-[hsl(var(--sidebar-border))] bg-[hsl(var(--sidebar-accent))] p-3 text-xs leading-5 text-[hsl(var(--primary-foreground)/.7)]"><ShieldCheck size={14} className="mr-1 inline text-[hsl(var(--accent))]" /> Demo prediction only. Verify with a local buyer before committing.</div></div></div>;
  if (section === 'demand') return <Demand selectedLocation={selectedLocation} setSelectedLocation={setSelectedLocation} highlightHighDemand={search.get('highlight') === 'high'} />;
  if (section === 'prices') return <div className="grid gap-5 lg:grid-cols-[1.25fr_.75fr]"><div className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5 sm:p-7"><div className="mb-5 flex items-start justify-between"><div><p className="font-mono text-[10px] uppercase tracking-[.15em] text-[hsl(var(--muted-foreground))]">Last 6 months</p><h2 className="mt-1 font-display text-2xl">Tomato / Rice / Chilli</h2></div><Status tone="amber">Mock market values</Status></div><div className="h-72"><ResponsiveContainer width="100%" height="100%"><AreaChart data={priceHistory}><CartesianGrid strokeDasharray="3 3" stroke="#dfe5d9" vertical={false} /><XAxis dataKey="month" tick={{ fontSize: 11 }} axisLine={false} tickLine={false} /><YAxis tick={{ fontSize: 10 }} axisLine={false} tickLine={false} /><Tooltip /><Area type="monotone" dataKey="Tomato" stroke="#487c59" fill="#dcebd9" strokeWidth={3} /><Area type="monotone" dataKey="Rice" stroke="#c7a13f" fill="transparent" strokeWidth={2} /><Area type="monotone" dataKey="Chilli" stroke="#c56e55" fill="transparent" strokeWidth={2} /></AreaChart></ResponsiveContainer></div></div><div className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-6"><p className="font-mono text-[10px] uppercase tracking-[.15em] text-[hsl(var(--muted-foreground))]">AI outlook</p><h2 className="mt-1 font-display text-2xl">Hold tomato for 4–6 days</h2><p className="mt-3 text-sm leading-6 text-[hsl(var(--muted-foreground))]">The demo model sees high demand around Guntur and a possible ₹2–4/kg lift.</p><div className="mt-6 space-y-3">{[['Tomato', '₹30/kg', '+8.4%'], ['Rice', '₹42/kg', '+3.1%'], ['Chilli', '₹118/kg', '+12.2%']].map(x => <div className="flex items-center justify-between rounded-xl bg-[hsl(var(--muted))] p-3" key={x[0]}><span className="text-sm font-bold">{x[0]}</span><span className="font-mono text-xs">{x[1]} <b className="ml-2 text-[hsl(var(--primary))]">{x[2]}</b></span></div>)}</div></div></div>;
  if (section === 'orders') return <OrderList show={show} filter={search.get('status') as 'ongoing' | 'delivered' | null} />;
  if (section === 'payments') return <PaymentLedger focusPending={search.get('status') === 'pending'} />;
  if (section === 'delivery') return <TrackingCard role="farmer" show={show} />;
  if (section === 'quality') return <Quality show={show} />;
  if (section === 'ratings') return <Ratings show={show} />;
  if (section === 'rewards') return <Rewards role="farmer" />;
  if (section === 'assistant') return <AssistantPage />;
  return <HelpPage show={show} />;
}

function Demand({ selectedLocation, setSelectedLocation, highlightHighDemand = false }: { selectedLocation: string; setSelectedLocation: (s: string) => void; highlightHighDemand?: boolean }) { const d = demandByLocation[selectedLocation]; return <div className="grid gap-5 lg:grid-cols-[.8fr_1.2fr]"><div className="rounded-2xl bg-[hsl(var(--primary))] p-6 text-[hsl(var(--primary-foreground))]"><p className="font-mono text-[10px] uppercase tracking-[.16em] text-[hsl(var(--accent))]">Choose a market</p><select value={selectedLocation} onChange={e => setSelectedLocation(e.target.value)} className="mt-3 w-full rounded-xl border border-[hsl(var(--sidebar-border))] bg-[hsl(var(--sidebar-accent))] px-3 py-3 text-sm" data-testid="select-market-location">{Object.keys(demandByLocation).map(l => <option key={l}>{l}</option>)}</select><p className="mt-4 text-sm text-[hsl(var(--primary-foreground)/.7)]">{d.market}</p><div className="mt-9 rounded-2xl border border-[hsl(var(--sidebar-border))] bg-[hsl(var(--sidebar-accent))] p-4"><p className="font-display text-3xl">High signal</p><p className="mt-1 text-xs text-[hsl(var(--primary-foreground)/.64)]">Updated 2 hours ago • simulated</p></div></div><div className="grid gap-3 sm:grid-cols-3">{[['High demand', d.high, 'bg-[#dcebd9]', 'text-[#2c6738]'], ['Medium', d.medium, 'bg-[#f5e8bc]', 'text-[#7e5d13]'], ['Low', d.low, 'bg-[#e5e9de]', 'text-[#66705f]']].map(([label, items, bg, text]) => <div className={`rounded-2xl p-5 ${bg} ${text} ${label === 'High demand' && highlightHighDemand ? 'ring-4 ring-[hsl(var(--accent))] ring-offset-2' : ''}`} key={String(label)} data-testid={label === 'High demand' ? 'panel-high-demand' : undefined}><p className="font-mono text-[10px] uppercase tracking-[.14em]">{label as string}</p><div className="mt-7 space-y-3">{(items as string[]).map(i => <p className="flex items-center gap-2 text-sm font-bold" key={i}><span className="h-1.5 w-1.5 rounded-full bg-current" />{i}</p>)}</div></div>)}</div></div>; }

function OrderList({ show, filter }: { show: (s: string) => void; filter?: 'ongoing' | 'delivered' | null }) { const visibleOrders = filter === 'ongoing' ? orders.filter(o => o.status !== 'Delivered') : filter === 'delivered' ? orders.filter(o => o.status === 'Delivered') : orders; return <div className="space-y-3">{filter && <div className="flex items-center gap-2 rounded-xl bg-[hsl(var(--secondary))] px-4 py-3 text-xs font-bold" data-testid={`status-filter-${filter}`}>{filter === 'ongoing' ? 'Ongoing Orders' : 'Delivered Orders'}<span className="ml-auto font-mono text-[hsl(var(--muted-foreground))]">{visibleOrders.length} shown</span></div>}{visibleOrders.map((o, i) => <div key={o.id} className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-4 sm:p-5" data-testid={`row-order-${o.id}`}><div className="flex flex-wrap items-center gap-3"><div className={`grid h-11 w-11 place-items-center rounded-xl ${i === 0 ? 'bg-[#efb3a3]' : i === 1 ? 'bg-[#e9b08c]' : 'bg-[#e8d58f]'} font-display text-xl`}>{o.crop[0]}</div><div className="min-w-[160px] flex-1"><p className="font-bold">{o.id} • {o.crop}</p><p className="text-xs text-[hsl(var(--muted-foreground))]">{o.buyer} • {o.quantity} • {o.date}</p></div><div className="text-left sm:text-right"><p className="font-mono text-sm font-bold">{o.total}</p><p className="text-[11px] text-[hsl(var(--muted-foreground))]">{o.paid}</p></div><Status tone={o.status === 'Delivered' ? 'green' : o.status === 'In Transit' ? 'amber' : 'blue'}>{o.status}</Status><button onClick={() => show(`Order ${o.id} details opened`)} className="rounded-lg border border-[hsl(var(--border))] p-2" data-testid={`button-order-details-${o.id}`}><ArrowRight size={15} /></button></div></div>)}</div>; }

function PaymentLedger({ focusPending = false }: { focusPending?: boolean }) { return <div className="grid gap-5 lg:grid-cols-[1.3fr_.7fr]"><div className={`overflow-hidden rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] ${focusPending ? 'ring-4 ring-[hsl(var(--accent))] ring-offset-2' : ''}`} data-testid={focusPending ? 'panel-pending-payments' : 'panel-payment-ledger'}><div className="flex items-center justify-between border-b border-[hsl(var(--border))] p-5"><h2 className="font-display text-2xl">Payment ledger</h2><Status tone="amber">₹32,000 pending</Status></div>{orders.map(o => <div className="flex flex-wrap items-center gap-3 border-b border-[hsl(var(--border))] p-5 last:border-0" key={o.id}><WalletCards size={18} className="text-[hsl(var(--primary))]" /><div className="min-w-[150px] flex-1"><p className="text-sm font-bold">{o.id} • {o.buyer}</p><p className="text-xs text-[hsl(var(--muted-foreground))]">{o.paid}</p></div><p className="font-mono text-sm">{o.total}</p><Status tone={o.paid.includes('40%') ? 'amber' : 'green'}>{o.paid.includes('40%') ? 'Awaiting balance' : 'Settled'}</Status></div>)}</div><div className="rounded-2xl bg-[hsl(var(--secondary))] p-6"><Banknote className="text-[hsl(var(--primary))]" /><p className="mt-6 font-display text-3xl">₹1,84,500</p><p className="mt-1 text-sm text-[hsl(var(--muted-foreground))]">Lifetime demo earnings</p><div className="mt-8 border-t border-[hsl(var(--border))] pt-4 text-xs text-[hsl(var(--muted-foreground))]">Payments are simulated for this prototype. No money moves.</div></div></div>; }

function TrackingCard({ role, show }: { role: Role; show: (s: string) => void }) { const [status, setStatus] = useState('In Transit'); const next = status === 'In Transit' ? 'Near Destination' : status === 'Near Destination' ? 'Delivered' : 'Delivered'; return <div className="grid gap-5 lg:grid-cols-[1fr_.8fr]"><div className="agri-grid rounded-2xl border border-[hsl(var(--border))] bg-[#edf1e7] p-6"><div className="flex items-center justify-between"><p className="font-mono text-[10px] uppercase tracking-[.15em]">Map-style route</p><Status tone="blue">Updated 2 min ago</Status></div><div className="relative mt-5 h-64"><div className="absolute left-[18%] top-[27%] grid h-7 w-7 place-items-center rounded-full bg-[hsl(var(--primary))] text-white shadow-[0_0_0_7px_hsl(var(--primary)/.15)]"><MapPin size={14} /></div><div className="absolute right-[18%] top-[67%] grid h-7 w-7 place-items-center rounded-full bg-[hsl(var(--accent))] text-[hsl(var(--primary))] shadow-[0_0_0_7px_hsl(var(--accent)/.3)]"><MapPin size={14} /></div><div className="absolute left-[23%] top-[34%] h-32 w-[52%] rotate-[22deg] border-b-2 border-dashed border-[hsl(var(--primary))]" /><p className="absolute left-[7%] top-[14%] text-[11px] font-bold">{role === 'farmer' ? 'Lakshya Farms' : 'Collection point'}</p><p className="absolute bottom-[12%] right-[6%] text-[11px] font-bold">Receiving bay</p></div><div className="grid grid-cols-3 gap-3 border-t border-[#d2dccc] pt-4 text-center"><div><p className="font-mono font-bold">18.4 km</p><p className="text-[10px] text-[hsl(var(--muted-foreground))]">distance</p></div><div><p className="font-mono font-bold">42 min</p><p className="text-[10px] text-[hsl(var(--muted-foreground))]">ETA</p></div><div><p className="font-mono font-bold">12°C</p><p className="text-[10px] text-[hsl(var(--muted-foreground))]">cold chain</p></div></div></div><div className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-6"><p className="font-mono text-[10px] uppercase tracking-[.15em] text-[hsl(var(--muted-foreground))]">#AG1024 timeline</p><h2 className="mt-1 font-display text-2xl">Tomato • 800 kg</h2><div className="mt-7 space-y-5">{['Assigned', 'Picked Up', 'In Transit', 'Near Destination', 'Delivered'].map((s, i) => <div className="flex items-center gap-3" key={s}><span className={`grid h-6 w-6 place-items-center rounded-full ${['Assigned', 'Picked Up', 'In Transit'].includes(s) || status === s ? 'bg-[hsl(var(--primary))] text-white' : 'bg-[hsl(var(--muted))]'}`}>{['Assigned', 'Picked Up', 'In Transit'].includes(s) || status === s ? <Check size={13} /> : <span className="h-1.5 w-1.5 rounded-full bg-current" />}</span><span className="text-sm">{s}</span><span className="ml-auto text-[11px] text-[hsl(var(--muted-foreground))]">{i < 3 ? ['08:10', '08:42', '09:10'][i] : '—'}</span></div>)}</div><button onClick={() => { setStatus(next); show(`Delivery updated to ${next}`); }} disabled={status === 'Delivered'} className="mt-7 w-full rounded-xl bg-[hsl(var(--primary))] py-3 text-xs font-bold text-[hsl(var(--primary-foreground))] disabled:opacity-50" data-testid="button-update-tracking">{status === 'Delivered' ? 'Delivered' : `Advance to ${next}`}</button></div></div>; }

function Quality({ show }: { show: (s: string) => void }) { return <div className="grid gap-5 lg:grid-cols-[.8fr_1.2fr]"><div className="rounded-2xl bg-[hsl(var(--primary))] p-7 text-[hsl(var(--primary-foreground))]"><BadgeCheck className="text-[hsl(var(--accent))]" size={25} /><p className="mt-7 font-mono text-[10px] uppercase tracking-[.16em] text-[hsl(var(--accent))]">Lakshya Farms</p><p className="mt-2 font-display text-5xl">94<span className="text-2xl">/100</span></p><p className="mt-2 text-sm text-[hsl(var(--primary-foreground)/.68)]">Verified quality score</p><button onClick={() => show('Verification certificate downloaded')} className="mt-8 rounded-xl bg-[hsl(var(--accent))] px-4 py-2.5 text-xs font-bold text-[hsl(var(--primary))]" data-testid="button-download-verification">Download certificate</button></div><div className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-6"><h2 className="font-display text-2xl">Verification signals</h2><div className="mt-6 space-y-4">{[['Identity verified', 'Aadhaar-linked demo profile'], ['Farm location checked', 'Guntur, Andhra Pradesh'], ['Produce photos reviewed', 'Tomato • Grade A'], ['Buyer history', '42 completed orders']].map(x => <div className="flex items-center gap-3" key={x[0]}><span className="grid h-8 w-8 place-items-center rounded-full bg-[#dcebd9] text-[#2c6738]"><Check size={15} /></span><div><p className="text-sm font-bold">{x[0]}</p><p className="text-xs text-[hsl(var(--muted-foreground))]">{x[1]}</p></div></div>)}</div><p className="mt-6 rounded-xl bg-[hsl(var(--muted))] p-3 text-[11px] leading-5 text-[hsl(var(--muted-foreground))]">Verification is a mock trust layer for this frontend demo.</p></div></div>; }

function Ratings({ show }: { show: (s: string) => void }) { const [rating, setRating] = useState(0); return <div className="grid gap-5 lg:grid-cols-[.8fr_1.2fr]"><div className="rounded-2xl bg-[hsl(var(--secondary))] p-7"><p className="font-mono text-[10px] uppercase tracking-[.16em] text-[hsl(var(--primary))]">Your reputation</p><p className="mt-3 font-display text-6xl">4.8</p><div className="mt-2 flex text-[hsl(var(--accent))]">{[1, 2, 3, 4, 5].map(i => <Star key={i} size={18} fill="currentColor" />)}</div><p className="mt-3 text-sm text-[hsl(var(--muted-foreground))]">From 38 buyer ratings</p></div><div className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-6"><h2 className="font-display text-2xl">Rate FreshMart Foods</h2><p className="mt-1 text-sm text-[hsl(var(--muted-foreground))]">Order #AG1024 • Tomato</p><div className="mt-6 flex gap-2">{[1, 2, 3, 4, 5].map(i => <button key={i} onClick={() => setRating(i)} className="rounded-lg p-2" data-testid={`button-rating-${i}`}><Star size={25} fill={i <= rating ? '#d8ae4c' : 'none'} className={i <= rating ? 'text-[#d8ae4c]' : 'text-[hsl(var(--border))]'} /></button>)}</div><div className="mt-5 grid gap-2 sm:grid-cols-2">{['Product Quality', 'Communication', 'Price', 'Delivery'].map(c => <button key={c} onClick={() => show(`${c} selected`)} className="rounded-xl border border-[hsl(var(--border))] p-3 text-left text-xs font-bold" data-testid={`button-rating-category-${c.toLowerCase().replaceAll(' ', '-')}`}>{c}<span className="float-right text-[hsl(var(--muted-foreground))]">Select</span></button>)}</div><button onClick={() => show(`Rating of ${rating || 5} stars submitted`)} className="mt-5 rounded-xl bg-[hsl(var(--primary))] px-4 py-2.5 text-xs font-bold text-[hsl(var(--primary-foreground))]" data-testid="button-submit-rating">Submit rating</button></div></div>; }

function Rewards({ role }: { role: Role }) { return <div className="grid gap-5 lg:grid-cols-[1fr_.8fr]"><div className="rounded-2xl bg-[hsl(var(--primary))] p-7 text-[hsl(var(--primary-foreground))]"><div className="flex items-center justify-between"><div><p className="font-mono text-[10px] uppercase tracking-[.16em] text-[hsl(var(--accent))]">Harvest rewards</p><p className="mt-2 font-display text-5xl">{role === 'farmer' ? '860' : '1,240'}</p><p className="mt-1 text-sm text-[hsl(var(--primary-foreground)/.65)]">points available</p></div><div className="grid h-16 w-16 place-items-center rounded-2xl bg-[hsl(var(--accent))] text-[hsl(var(--primary))]"><Star fill="currentColor" /></div></div><div className="mt-8"><div className="flex justify-between text-xs"><span>Next reward: market boost</span><span>72%</span></div><div className="mt-2 h-2 rounded-full bg-[hsl(var(--sidebar-accent))]"><div className="h-full w-[72%] rounded-full bg-[hsl(var(--accent))]" /></div></div></div><div className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-6"><h2 className="font-display text-2xl">Ways to earn</h2><div className="mt-5 space-y-3">{['Complete quality profile', 'Keep delivery updates current', 'Rate a completed order'].map((x, i) => <div className="flex items-center gap-3 rounded-xl bg-[hsl(var(--muted))] p-3" key={x}><span className="grid h-8 w-8 place-items-center rounded-full bg-[hsl(var(--accent))] font-mono text-xs">{[120, 80, 40][i]}</span><span className="text-xs font-bold">{x}</span><Check className="ml-auto text-[hsl(var(--primary))]" size={15} /></div>)}</div></div></div>; }

function AssistantPage() { return <div className="grid gap-5 lg:grid-cols-[.85fr_1.15fr]"><div className="rounded-2xl bg-[hsl(var(--primary))] p-7 text-[hsl(var(--primary-foreground))]"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-[hsl(var(--accent))] text-[hsl(var(--primary))]"><Bot /></div><h2 className="mt-7 font-display text-4xl">Ask the field desk.</h2><p className="mt-3 text-sm leading-6 text-[hsl(var(--primary-foreground)/.7)]">AgriBot can explain the market signal, help format a listing, or connect you to a volunteer. Its answers are simulated for this demo.</p><button className="mt-7 rounded-xl bg-[hsl(var(--accent))] px-4 py-2.5 text-xs font-bold text-[hsl(var(--primary))]" data-testid="button-open-assistant">Start a conversation</button></div><div className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-6"><p className="font-mono text-[10px] uppercase tracking-[.16em] text-[hsl(var(--muted-foreground))]">Suggested questions</p><div className="mt-5 grid gap-2 sm:grid-cols-2">{['What is tomato demand in Guntur?', 'How do I prepare an order?', 'Find a buyer for chilli', 'Request a volunteer'].map(q => <Link href={q.includes('volunteer') ? '/farmer/help' : '/farmer/dashboard'} key={q} className="rounded-xl border border-[hsl(var(--border))] p-4 text-sm font-bold hover:bg-[hsl(var(--secondary))]" data-testid={`link-assistant-${q.slice(0, 8).replaceAll(' ', '-')}`}>{q}<ArrowRight className="mt-2 text-[hsl(var(--primary))]" size={15} /></Link>)}</div></div></div>; }

function HelpPage({ show }: { show: (s: string) => void }) { return <div className="grid gap-5 sm:grid-cols-3"><button onClick={() => show('Volunteer request created')} className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-6 text-left hover:-translate-y-0.5" data-testid="button-request-volunteer"><HandHelping className="text-[hsl(var(--primary))]" /><h2 className="mt-8 font-display text-2xl">Request a volunteer</h2><p className="mt-2 text-sm text-[hsl(var(--muted-foreground))]">A patient human can walk through the next step with you.</p></button><button onClick={() => show('Call simulation started')} className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-6 text-left hover:-translate-y-0.5" data-testid="button-call-support"><Phone className="text-[hsl(var(--primary))]" /><h2 className="mt-8 font-display text-2xl">Call support</h2><p className="mt-2 text-sm text-[hsl(var(--muted-foreground))]">A simulated support line is ready for the demo.</p></button><button onClick={() => show('Chat support opened')} className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-6 text-left hover:-translate-y-0.5" data-testid="button-chat-support"><MessageCircle className="text-[hsl(var(--primary))]" /><h2 className="mt-8 font-display text-2xl">Chat with AgriBot</h2><p className="mt-2 text-sm text-[hsl(var(--muted-foreground))]">Quick answers for price, payment, buyers, and delivery.</p></button></div>; }

function ConsumerSection({ section, cart, setCart, paid, setPaid, show }: { section: string; cart: { id: string; qty: number }[]; setCart: (x: { id: string; qty: number }[]) => void; paid: boolean; setPaid: (x: boolean) => void; show: (s: string) => void }) {
  if (section === 'shop') { const total = cart.reduce((sum, item) => sum + (products.find(p => p.id === item.id)?.price || 0) * item.qty, 0); return <div className="grid gap-5 xl:grid-cols-[1fr_.35fr]"><div><div className="mb-4 flex items-center gap-2 overflow-auto"><span className="rounded-full bg-[hsl(var(--primary))] px-3 py-1.5 text-xs font-bold text-white">All produce</span>{['Vegetables', 'Grains', 'Spices', 'Pantry'].map(x => <button key={x} className="whitespace-nowrap rounded-full border border-[hsl(var(--border))] px-3 py-1.5 text-xs font-bold">{x}</button>)}</div><div className="grid gap-4 sm:grid-cols-2">{products.map(p => <div className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-4" key={p.id} data-testid={`card-product-${p.id}`}><div className={`grid h-40 place-items-center rounded-2xl ${p.color} font-display text-6xl text-[#4d5140]`}>{p.name[0]}</div><div className="mt-4 flex justify-between gap-3"><div><p className="font-bold">{p.name}</p><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">{p.farmer} • {p.unit}</p></div><p className="font-mono text-sm font-bold">{money(p.price)}</p></div><p className="mt-3 text-xs text-[hsl(var(--primary))]">{p.note}</p><div className="mt-4 flex gap-2"><button onClick={() => { const item = cart.find(x => x.id === p.id); setCart(item ? cart.map(x => x.id === p.id ? { ...x, qty: x.qty + 1 } : x) : [...cart, { id: p.id, qty: 1 }]); show(`${p.name} added to basket`); }} className="flex-1 rounded-xl bg-[hsl(var(--primary))] py-2.5 text-xs font-bold text-[hsl(var(--primary-foreground))]" data-testid={`button-add-${p.id}`}>Add to basket</button><button onClick={() => { setCart([{ id: p.id, qty: 1 }]); show('Buy now selected'); }} className="rounded-xl border border-[hsl(var(--border))] px-3 text-xs font-bold" data-testid={`button-buy-${p.id}`}>Buy now</button></div></div>)}</div></div><div className="h-fit rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5 xl:sticky xl:top-24"><div className="flex items-center justify-between"><h2 className="font-display text-2xl">Your basket</h2><span className="rounded-full bg-[hsl(var(--secondary))] px-2 py-1 font-mono text-[10px]">{cart.reduce((a, x) => a + x.qty, 0)}</span></div>{cart.length === 0 ? <div className="py-12 text-center text-sm text-[hsl(var(--muted-foreground))]">Your basket is waiting for something fresh.</div> : <><div className="mt-5 space-y-3">{cart.map(item => { const p = products.find(x => x.id === item.id)!; return <div className="flex items-center gap-2" key={item.id}><div className={`grid h-9 w-9 place-items-center rounded-lg ${p.color} font-display`}>{p.name[0]}</div><div className="min-w-0 flex-1"><p className="truncate text-xs font-bold">{p.name}</p><p className="font-mono text-[10px]">{money(p.price)} × {item.qty}</p></div><button onClick={() => setCart(cart.filter(x => x.id !== item.id))} className="text-xs text-[hsl(var(--muted-foreground))]" data-testid={`button-remove-${item.id}`}>Remove</button></div>; })}</div><div className="mt-5 flex justify-between border-t border-[hsl(var(--border))] pt-4 text-sm font-bold"><span>Total</span><span className="font-mono">{money(total)}</span></div><button onClick={() => { setPaid(true); show('Payment Successful • Order Confirmed'); }} className="mt-4 w-full rounded-xl bg-[hsl(var(--primary))] py-3 text-xs font-bold text-[hsl(var(--primary-foreground))]" data-testid="button-checkout">Pay total amount</button>{paid && <div className="mt-3 rounded-xl bg-[#dcebd9] p-3 text-xs font-bold text-[#2c6738]" data-testid="status-payment-success"><Check size={14} className="mr-1 inline" /> Payment Successful • Order Confirmed</div>}</>}</div></div>; }
  if (section === 'delivery') return <TrackingCard role="consumer" show={show} />;
  if (section === 'ratings') return <Ratings show={show} />;
  if (section === 'rewards') return <Rewards role="consumer" />;
  if (section === 'assistant') return <HelpPage show={show} />;
  if (section === 'savings') return <div className="grid gap-5 sm:grid-cols-3"><StatCard label="Saved this month" value="₹486" note="Compared with retail estimates" icon={CircleDollarSign} /><StatCard label="Direct-farm share" value="82%" note="Of your basket value" /><StatCard label="Orders without middle layers" value="11" note="Since joining" /></div>;
  return <OrderList show={show} />;
}

function BulkSection({ section, paid, setPaid, advance, setAdvance, show }: { section: string; paid: boolean; setPaid: (x: boolean) => void; advance: boolean; setAdvance: (x: boolean) => void; show: (s: string) => void }) {
  if (section === 'payments') { const now = advance ? 48000 : 120000; return <div className="grid gap-5 lg:grid-cols-[1fr_.75fr]"><div className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-6"><div className="flex items-center justify-between"><div><p className="font-mono text-[10px] uppercase tracking-[.16em] text-[hsl(var(--muted-foreground))]">Mock invoice / BLK2042</p><h2 className="mt-1 font-display text-2xl">Tomato • 4,000 kg</h2></div><Status tone="amber">Payment due</Status></div><div className="mt-7 rounded-xl bg-[hsl(var(--muted))] p-4"><div className="flex justify-between text-sm"><span>Order value</span><b className="font-mono">₹1,20,000</b></div><div className="mt-3 flex justify-between text-xs text-[hsl(var(--muted-foreground))]"><span>{advance ? '40% advance now' : 'Full amount now'}</span><span className="font-mono">{money(now)}</span></div>{advance && <div className="mt-2 flex justify-between text-xs text-[hsl(var(--muted-foreground))]"><span>Remaining after delivery</span><span className="font-mono">₹72,000</span></div>}</div><div className="mt-5 grid gap-2 sm:grid-cols-2"><button onClick={() => setAdvance(true)} className={`rounded-xl border p-4 text-left ${advance ? 'border-[hsl(var(--primary))] bg-[hsl(var(--secondary))]' : 'border-[hsl(var(--border))]'}`} data-testid="button-pay-advance"><p className="text-sm font-bold">Pay 40% Advance</p><p className="mt-1 font-mono text-xs">₹48,000 now / ₹72,000 remaining</p></button><button onClick={() => setAdvance(false)} className={`rounded-xl border p-4 text-left ${!advance ? 'border-[hsl(var(--primary))] bg-[hsl(var(--secondary))]' : 'border-[hsl(var(--border))]'}`} data-testid="button-pay-full"><p className="text-sm font-bold">Pay Full Amount</p><p className="mt-1 font-mono text-xs">₹1,20,000 now / ₹0 remaining</p></button></div><div className="mt-5 flex gap-2">{['UPI', 'Bank Transfer', 'Digital Wallet'].map((x, i) => <button key={x} className="flex-1 rounded-xl border border-[hsl(var(--border))] px-2 py-3 text-[11px] font-bold" onClick={() => show(`${x} selected`)} data-testid={`button-payment-${i}`}>{i === 0 ? <Smartphone size={15} className="mx-auto mb-1" /> : i === 1 ? <Banknote size={15} className="mx-auto mb-1" /> : <WalletCards size={15} className="mx-auto mb-1" />}{x}</button>)}</div><button onClick={() => { setPaid(true); show('Payment Successful • Bulk order confirmed'); }} className="mt-5 w-full rounded-xl bg-[hsl(var(--primary))] py-3 text-sm font-bold text-[hsl(var(--primary-foreground))]" data-testid="button-pay-bulk">{paid ? 'Payment Successful • Order Confirmed' : `Pay ${money(now)} now`}</button></div><div className="rounded-2xl bg-[hsl(var(--primary))] p-6 text-[hsl(var(--primary-foreground))]"><CreditCard /><h2 className="mt-8 font-display text-3xl">Your choice, clearly.</h2><p className="mt-3 text-sm leading-6 text-[hsl(var(--primary-foreground)/.68)]">Bulk buyers can protect cash flow with a 40% advance, or close the loop in one payment. This is a simulated flow.</p></div></div>; }
  if (section === 'farmers') return <div className="grid gap-4 sm:grid-cols-2">{[['Lakshya Farms', 'Guntur', 'Tomato • Chilli • Rice', '4.9'], ['Savitri Organics', 'Tenali', 'Rice • Groundnut', '4.7'], ['Ravi Growers', 'Kurnool', 'Cotton • Maize', '4.8'], ['Meena Harvest', 'Vijayawada', 'Tomato • Rice', '4.6']].map(x => <div className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5" key={x[0]}><div className="flex items-start justify-between"><div className="grid h-11 w-11 place-items-center rounded-xl bg-[hsl(var(--secondary))] font-mono text-xs font-bold">{String(x[0]).split(' ').map(s => s[0]).join('')}</div><Status><BadgeCheck size={12} /> Verified</Status></div><h2 className="mt-5 font-display text-2xl">{x[0]}</h2><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]"><MapPin size={12} className="mr-1 inline" />{x[1]} • {x[2]}</p><div className="mt-5 flex items-center justify-between border-t border-[hsl(var(--border))] pt-4"><span className="flex items-center gap-1 text-xs font-bold text-[hsl(var(--accent-foreground))]"><Star size={14} fill="#d8ae4c" className="text-[#d8ae4c]" /> {x[3]}</span><button onClick={() => show(`Conversation with ${x[0]} started`)} className="rounded-lg bg-[hsl(var(--secondary))] px-3 py-2 text-xs font-bold text-[hsl(var(--primary))]" data-testid={`button-contact-${String(x[0]).replaceAll(' ', '-')}`}>Contact farmer</button></div></div>)}</div>;
  if (section === 'requirements') return <div className="grid gap-4 sm:grid-cols-2">{[['Tomato', '4,000 kg', 'Needed by 12 Sept', 'Matching'], ['Rice', '2,500 kg', 'Needed by 20 Sept', 'Open'], ['Chilli', '600 kg', 'Needed by 18 Sept', '2 offers']].map(x => <div className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5" key={x[0]}><div className="flex justify-between"><h2 className="font-display text-2xl">{x[0]}</h2><Status tone={x[3] === 'Open' ? 'blue' : 'amber'}>{x[3]}</Status></div><p className="mt-2 font-mono text-sm">{x[1]}</p><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">{x[2]} • Hyderabad receiving</p><button onClick={() => show('Requirement editing enabled')} className="mt-5 text-xs font-bold text-[hsl(var(--primary))]" data-testid={`button-edit-requirement-${x[0]}`}>Edit requirement <ArrowRight size={13} className="inline" /></button></div>)}</div>;
  if (section === 'delivery') return <TrackingCard role="bulk" show={show} />;
  if (section === 'ratings') return <Ratings show={show} />;
  return <OrderList show={show} />;
}

function DeliverySection({ section, show }: { section: string; show: (s: string) => void }) { if (section === 'active') return <TrackingCard role="delivery" show={show} />; return <div className="space-y-3">{orders.slice(0, 3).map(o => <div className="flex flex-wrap items-center gap-3 rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5" key={o.id}><PackageCheck className="text-[hsl(var(--primary))]" /><div className="flex-1"><p className="font-bold">{o.id} • {o.crop}</p><p className="text-xs text-[hsl(var(--muted-foreground))]">{o.buyer} • delivered {o.date}</p></div><Status>Delivered</Status><button onClick={() => show('Delivery proof opened')} className="rounded-lg border border-[hsl(var(--border))] p-2" data-testid={`button-proof-${o.id}`}><FileText size={15} /></button></div>)}</div>; }
function VolunteerSection({ section, show }: { section: string; show: (s: string) => void }) { if (section === 'requests') return <div className="grid gap-3">{[['Ravi Naidu', 'Needs help uploading crop photos', 'Kurnool • 9 min ago'], ['Anitha Devi', 'Wants to understand pending payment', 'Guntur • 22 min ago'], ['Bharat Kumar', 'Cannot find delivery status', 'Tenali • 31 min ago']].map((x, i) => <div className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5" key={x[0]}><div className="flex items-start gap-3"><div className="grid h-10 w-10 place-items-center rounded-full bg-[hsl(var(--secondary))] font-mono text-xs font-bold">{String(x[0]).split(' ').map(s => s[0]).join('')}</div><div className="flex-1"><p className="font-bold">{x[0]}</p><p className="mt-1 text-sm">{x[1]}</p><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">{x[2]}</p></div><Status tone="amber">Pending</Status></div><div className="mt-4 flex gap-2"><button onClick={() => show(`Calling ${x[0]}`)} className="rounded-lg bg-[hsl(var(--primary))] px-3 py-2 text-xs font-bold text-[hsl(var(--primary-foreground))]" data-testid={`button-vol-call-${i}`}><Phone size={13} className="mr-1 inline" /> Call farmer</button><button onClick={() => show('Request accepted')} className="rounded-lg border border-[hsl(var(--border))] px-3 py-2 text-xs font-bold" data-testid={`button-vol-accept-${i}`}>Accept request</button><button onClick={() => show('Request marked completed')} className="rounded-lg border border-[hsl(var(--border))] px-3 py-2 text-xs font-bold" data-testid={`button-vol-complete-${i}`}>Complete</button></div></div>)}</div>; return <div className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-6"><h2 className="font-display text-2xl">26 completed calls</h2><p className="mt-2 text-sm text-[hsl(var(--muted-foreground))]">Your support history is a simulated community record for this demo.</p></div>; }

const money = (n: number) => `₹${n.toLocaleString('en-IN')}`;